[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rl-healthcare-actions"
version = "1.0.0"
description = "Offline RL for healthcare intervention recommendations"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
